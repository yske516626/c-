#include<stdio.h>
void swap(int* pa, int* pb)
{
	int tam = 0;
	tam = *pa;
	*pa = *pb;
	*pb = tam;
}
int main(void)
{
	int a = 0;
	int b = 0;
	scanf("%d%d", &a, &b);
	printf("未交换前的数值：a = %d,b = %d\n", a, b);
	swap(&a, &b);
	printf("交换后：a = %d,b = %d\n", a, b);
	return 0;
	
}
